fn main() {
    let test = 5;

    if test > 0 {
        const other = if test == 5 then 3 else 4;
        const other_example = if test == 5 {
            use 3;
        } else {
            use 4;
        }
    }

    // This is a comment
    const test = "Hello, World"

    return 45;
}